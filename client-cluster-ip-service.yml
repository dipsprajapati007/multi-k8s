apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP 
  selector:
    component: web
  ports:
    - port: 3000
      targetPort: 3000

#We are not specifying nodePort here as we are not allowing 
#to connect to this service from outside
